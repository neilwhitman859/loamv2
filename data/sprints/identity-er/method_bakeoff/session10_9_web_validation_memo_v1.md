# Session 10.9 - web validation memo

## Recommendation

Withdraw the current recommendation of `hybrid_guarded_fils_person_alias_v1` as the best trusted path.

Replace it with:

> benchmark-clear provisional artifact, not independently validated, and currently challenged by fresh web evidence

## Why

The prior recommendation depended on three late zero-cost overlays that turned a guarded benchmark pass into a perfect benchmark pass.

After targeted web validation:

- `place_alias` looks externally supported.
- `maison_alias` does **not**.
- `fils_person_alias` is still not independently clean enough to trust.

The critical blocker is `Ardhuy Cabotte` / `de la Cabotte`.

Fresh web evidence no longer supports that pair as a reliable `MERGE`. Instead it points toward:

- Rhône `Domaine la Cabotte` under Marie-Pierre Plumet d'Ardhuy, documented on the official Cabotte and Ardhuy sites.
- Burgundy `Maison la Cabotte` appearing as a separate Burgundy line tied to Nicolas Potel in current trade material.

That is enough to break trust in the exact late overlay family that made the stack benchmark-perfect.

## Practical Consequence

Do not spend more time trying to "confirm" the current recommended stack as if it were almost over the line.

The honest next step is:

1. repair the truth status and entity histories around the challenged late benchmark wins
2. re-evaluate whether the late overlays belong in the method at all
3. only then resume method selection

## References

- Evidence log: `data/sprints/identity-er/method_bakeoff/session10_9_web_validation_ledger_v1.md`
- Local pair snapshots: `data/sprints/identity-er/method_bakeoff/session10_9_case_snapshots_v1.json`
